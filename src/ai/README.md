# AI Модуль

Эта папка содержит все файлы для работы с Gemini AI.

## Структура

### 📝 `prompts.js`
Содержит все промпты для AI запросов.

**Как редактировать промпты:**
1. Откройте файл `prompts.js`
2. Найдите нужную функцию (например, `generatePoetBioPrompt`)
3. Измените текст промпта
4. Сохраните файл
5. Изменения применятся автоматически

**Доступные промпты:**
- `generatePoetBioPrompt(poetName)` - генерация досье поэта
- `generatePoetStyleAnalysisPrompt(poetName)` - анализ стиля (пока не используется)
- `comparePoetsPrompt(poet1, poet2)` - сравнение поэтов (пока не используется)

### 🤖 `gemini.js`
Содержит логику работы с Gemini API.

**Основные функции:**
- `generateContent(prompt, temperature)` - генерация контента по промпту
- `generateAIRating(prompt)` - генерация AI-рейтинга с temperature = 0
- `checkAPIAvailability()` - проверка доступности API

**Настройки:**
- Модель: `gemini-flash-latest` (можно изменить в константе `MODEL_NAME`)
- API ключ: берется из `.env` файла (`VITE_GEMINI_API_KEY`)
- Температура: `0.2` (можно изменить в константе `GENERATION_TEMPERATURE`)

**Temperature (температура генерации):**

Temperature контролирует "креативность" и "случайность" ответов AI:

- **0.0-0.3** (рекомендуется для биографий):
  - ✅ Детерминированные, предсказуемые ответы
  - ✅ Факты, даты, структурированные данные
  - ✅ Меньше "выдумок", больше точности
  - Используется по умолчанию для биографий, влияния, драмы, красоты

- **0.5-0.7** (сбалансированный):
  - Умеренная креативность
  - Описания, объяснения

- **0.8-1.0** (креативный):
  - 🎨 Максимальная непредсказуемость
  - 🎨 Художественные тексты, стихи
  - 🎨 Разнообразные, неожиданные формулировки

**Усреднение для AI-рейтингов:**

Для AI-рейтингов используется специальная функция `generateAIRating()`, которая:
- ✅ Делает **3 запроса** к AI с temperature = 0
- ✅ **Усредняет результаты** всех запросов
- ✅ **Округляет до ближайшего 0.5** (3.2 → 3.0, 3.3 → 3.5, 3.7 → 4.0)
- ✅ Минимизирует случайность - один "неудачный" запрос не испортит оценку

**Почему 3 запроса:**
- Уменьшает влияние случайных отклонений
- Более справедливые и стабильные оценки
- Оптимальный баланс между точностью и скоростью

Это обеспечивает максимальную справедливость - AI оценивает объективно, а усреднение убирает случайность!

## Как добавить новый AI запрос

### Шаг 1: Добавьте промпт в `prompts.js`

```javascript
export const myNewPrompt = (parameter) => {
  return `Ваш промпт с ${parameter}`;
};
```

### Шаг 2: Используйте в компоненте

```javascript
import { generateContent } from '../ai/gemini';
import { myNewPrompt } from '../ai/prompts';

const myFunction = async () => {
  const prompt = myNewPrompt('значение');
  
  // Используем температуру по умолчанию (0.2)
  const result = await generateContent(prompt);
  
  // Или задаём свою температуру для креативных задач
  const creativeResult = await generateContent(prompt, 0.8);
  
  console.log(result);
};

// Для AI-рейтингов используйте специальную функцию
const generateRating = async (poetName) => {
  const prompt = generateAIRatingPrompt(poetName);
  
  // Генерация с 3-кратным усреднением для справедливости
  // parseAIRating передаётся как функция для парсинга каждого ответа
  const ratings = await generateAIRating(prompt, parseAIRating);
  
  console.log(ratings); // { creativity: 4.5, influence: 4, drama: 3.5, beauty: 3 }
  // Эти оценки - усреднённые из 3 запросов!
};
```

## Лимиты Gemini API (бесплатный tier)

- ✅ **1500 запросов в день**
- ✅ **15 запросов в минуту**
- ✅ **1 миллион токенов в день**

## Обработка ошибок

Модуль `gemini.js` автоматически обрабатывает ошибки:
- `API_KEY_INVALID` - неверный ключ
- `PERMISSION_DENIED` / `403` - API не активирован
- `RESOURCE_EXHAUSTED` / `429` - превышен лимит
- `404` - модель не найдена

Все ошибки возвращаются с понятными сообщениями на русском языке.

